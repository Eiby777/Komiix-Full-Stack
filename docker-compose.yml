version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - komiix-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    command: redis-server --maxmemory 200mb --maxmemory-policy allkeys-lru

  libretranslate:
    image: libretranslate/libretranslate
    ports:
      - "5000:5000"
    environment:
      - LT_LOAD_ONLY=es,en,zh-Hans,ko,ja
      - LT_THREADS=2
      - LT_UPDATE_MODELS=true
    networks:
      - komiix-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G  # Aumentado para descarga de modelos
        reservations:
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/translate", "-H", "Content-Type: application/json", "-d", "{\"q\":\"test\",\"source\":\"en\",\"target\":\"es\"}"]
      interval: 60s  # Más tiempo entre checks
      timeout: 30s   # Timeout más largo
      retries: 10    # Más reintentos
      start_period: 300s  # 5 minutos para que descargue modelos

  komiix-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    environment:
      - PYTHONPATH=/app
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CORS_ORIGINS=${CORS_ORIGINS}
      - SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET}
      - COMPOSE_BAKE=${COMPOSE_BAKE}
      - LIBRETRANSLATE_URL=http://libretranslate:5000
      # Configuraciones adicionales para estabilidad
      - UVICORN_WORKERS=1
      - UVICORN_WORKER_CONNECTIONS=1000
      - UVICORN_KEEPALIVE=2
    command: >
      sh -c "sleep 30 && 
             uvicorn server:app 
             --host 0.0.0.0 
             --port 8000 
             --workers 1
             --worker-connections 1000
             --keepalive-timeout 2
             --timeout-keep-alive 2
             --access-log
             --log-level info"
    networks:
      - komiix-network
    depends_on:
      libretranslate:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 120s  # Más tiempo porque espera a libretranslate

  komiix-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
        - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
        - VITE_KOMIIX_BACKEND_URL=${VITE_KOMIIX_BACKEND_URL}
      shm_size: 2gb
    ports:
      - "3000:80"
    restart: unless-stopped
    container_name: komiix-frontend
    environment:
      - COMPOSE_BAKE=${COMPOSE_BAKE}
      # Nginx optimizations
      - NGINX_WORKER_PROCESSES=auto
      - NGINX_WORKER_CONNECTIONS=1024
    networks:
      - komiix-network
    depends_on:
      komiix-backend:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    ulimits:
      nofile:
        soft: 65536
        hard: 65536

networks:
  komiix-network:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: komiix-br0
    ipam:
      driver: default
      config:
        - subnet: 172.20.0.0/16
